spring:
  datasource:
    url: jdbc:postgresql://notUsed:notUsed/notUsed
    username: notUsed
    password: notUsed
  jpa:
    hibernate:
      ddl-auto: create-drop

um:
  warsaw:
    api-key: ${UM_WARSAW_API_KEY}

logging:
  level:
    pl.nextleveldev.smart_route: debug